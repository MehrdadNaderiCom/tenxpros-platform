# Secrets — never commit these.
.env
.env.local
.env.*.local
.env.production
.env.production.local
.env.staging

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local logs
*.log

# Database backups / dumps — may contain password hashes; never commit.
backups/
scratch_academy/
